import React from "react";
import { usePost!Model!ByFieldMutation } from "../../store/apiSlice"; 
import GenericSelect from "../../common/GenericSelect";

const Select!Model! = ({ value, labelKey, onSelect,disabled, multiple }) => {
  return (
    <GenericSelect
      value={value}
      onSelect={onSelect}
      useQuery={()=>usePost!Model!ByFieldMutation()} 
      valueKey="_id"
      labelKey={labelKey}
      disabled={disabled}
      placeholder="Seleccione una Opción" 
      multiple={multiple} // Habilitar selección múltiple
    />
  );
};

export default Select!Model!;
